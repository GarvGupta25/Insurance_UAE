import { useState } from 'react';
import { Link, NavLink } from 'react-router-dom';
import { Compass, Menu, X, ArrowRight } from 'lucide-react';
const NAV_LINKS = [{ to: '/', label: 'Home' }, { to: '/plans', label: 'Popular Schemes' }, { to: '/guide', label: 'New to the UAE?' }, { to: '/about', label: 'About' }, { to: '/partners', label: 'Partners' }, { to: '/faq', label: 'FAQ' }, { to: '/contact', label: 'Contact' }];
export function MarketingHeader() { const [menuOpen, setMenuOpen] = useState(false); return <header className="mh-header"><div className="mh-header__inner m-page-shell"><Link to="/" className="brand" aria-label="Helm AI home"><span><Compass size={25} /></span>helm<span className="brand-ai">ai</span></Link><nav className="mh-header__nav" aria-label="Main navigation">{NAV_LINKS.map(({ to, label }) => <NavLink key={to} to={to} end={to === '/'} className={({ isActive }) => isActive ? 'mh-header__link mh-header__link--active' : 'mh-header__link'} onClick={() => setMenuOpen(false)}>{label}</NavLink>)}</nav><div className="mh-header__actions"><Link to="/login" className="m-button-secondary mh-header__auth-btn">Sign in</Link><Link to="/login" className="m-button-primary mh-header__auth-btn">Get started <ArrowRight size={16} /></Link></div><button type="button" className="mh-header__hamburger" onClick={() => setMenuOpen(!menuOpen)} aria-label={menuOpen ? 'Close navigation' : 'Open navigation'} aria-expanded={menuOpen}>{menuOpen ? <X size={24} /> : <Menu size={24} />}</button></div>{menuOpen && <div className="mh-mobile-panel" role="dialog" aria-label="Navigation menu"><nav>{NAV_LINKS.map(({ to, label }) => <NavLink key={to} to={to} end={to === '/'} className={({ isActive }) => isActive ? 'mh-mobile-panel__link mh-mobile-panel__link--active' : 'mh-mobile-panel__link'} onClick={() => setMenuOpen(false)}>{label}</NavLink>)}</nav><div className="mh-mobile-panel__actions"><Link to="/login" className="m-button-secondary" onClick={() => setMenuOpen(false)} style={{ width: '100%' }}>Sign in</Link><Link to="/login" className="m-button-primary" onClick={() => setMenuOpen(false)} style={{ width: '100%' }}>Get started <ArrowRight size={16} /></Link></div></div>}</header>; }
