# Phase 1 reviewer decision

Every application now enters `awaiting_broker_review`; confirmation requires an approved recommendation. An unassigned member sees the honest pending state and cannot self-approve. This avoids silently assigning a reviewer and preserves the existing administrator-managed broker-assignment model.
